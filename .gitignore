# PlatformIO
.pio/
.vscode/
compile_commands.json

# Lokale Entwicklerkonfiguration. Echte Zugangsdaten (WLAN-PSK, Syslog-IP, ...)
# leben zur Laufzeit in config.xml auf LittleFS, nie im Quellcode - dies ist nur
# ein Sicherheitsnetz, falls waehrend der Entwicklung testweise etwas in
# config.h landet, bevor die Einstellungsseite (P2/P5) existiert.
# "**/" noetig, da das Firmware-Projekt in firmware/include/ liegt, nicht im Repo-Root
**/include/config.h

# OS
Thumbs.db
.DS_Store
