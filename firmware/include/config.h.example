#pragma once

// ============================================================================
// Kopiere diese Datei nach "config.h" (wird von Git ignoriert, siehe .gitignore).
//
// Laufzeitkonfiguration (Netzwerk, Passwoerter, Sensor2, Syslog-Server, ...)
// kommt gemaess Lastenheft aus config.xml auf LittleFS (siehe ConfigManager,
// Phase P2) - NICHT aus dieser Datei. Hier stehen nur Werte, die zur
// Kompilierzeit feststehen muessen.
// ============================================================================

#define DEVICE_FIRMWARE_VERSION "0.1.0-p5"

// GitHub-Repo fuer den OTA-Versionscheck (siehe OtaManager). Kein Geheimnis,
// nur zur Kompilierzeit fix, weil es sich praktisch nie aendert.
#define OTA_GITHUB_OWNER "peterhagelhof7-cmd"
#define OTA_GITHUB_REPO "sensormeter"
